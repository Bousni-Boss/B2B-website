'use client';import Link from 'next/link';import { usePathname,useRouter } from 'next/navigation';export default function NavBar({role}:{role?:string}){const pathname=usePathname();const r=useRouter();async function signout(){await fetch('/api/logout',{method:'POST'});r.push('/login');}return(<header style={{borderBottom:'1px solid var(--ring)',position:'sticky',top:0,background:'rgba(15,17,21,.8)',backdropFilter:'blur(8px)',zIndex:10}}><div className="container" style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'14px 24px'}}><div style={{fontWeight:700,letterSpacing:'.3px'}}>B2B Portal</div><nav style={{display:'flex',gap:'8px'}}><Link className='badge' href='/catalog'>Catalog</Link><Link className='badge' href='/custom'>Custom</Link>{role&&(role==='admin'||role==='superadmin')&&<Link className='badge' href='/admin'>Admin</Link>}<button className='button button-primary' onClick={signout}>Sign out</button></nav></div></header>);}