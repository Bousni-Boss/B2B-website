'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';export default function LoginPage(){const [u,setU]=useState('');const [p,setP]=useState('');const [err,setErr]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();setErr('');const res=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:u,password:p})});if(res.ok)r.push('/catalog');else setErr('Invalid credentials');}return(<main className='container'><div className='card' style={{maxWidth:420,margin:'80px auto'}}><h1>Sign in</h1><p style={{color:'var(--muted)',marginBottom:12}}>Use your company-provided account.</p><form onSubmit={submit} className='space-y-3'><input className='input' placeholder='Username' value={u} onChange={e=>setU(e.target.value)}/><input className='input' placeholder='Password' type='password' value={p} onChange={e=>setP(e.target.value)}/>{err&&<div className='badge' style={{borderColor:'var(--danger)',color:'var(--danger)'}}>{err}</div>}<button className='button button-primary' type='submit'>Sign in</button></form></div></main>);}