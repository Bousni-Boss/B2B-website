export const DEFAULT_CATEGORY="Autre";export const CATEGORY_PREFIX_MAP:Record<string,string>={BR:"Bracelets",BA:"Bague",GR:"Gourmette",CL:"Collier",CH:"Chaine",CK:"Choker",KH:"Khelkhal/خلخال",BZ:"Bzim",NB:"N'bala",MD:"M'demma",M:"Mariage",EN:"Ensemble",BC:"Boucle",PE:"Pendentifs",DI:"Autre"};export const CATEGORY_ORDER=["Bracelets","Bague","Gourmette","Collier","Chaine","Choker","Khelkhal/خلخال","Mariage","Ensemble","Boucle","Pendentifs","Bzim","N'bala","M'demma","Autre"];export function getPrefix(ref:string){const m=(ref||'').trim().match(/^[A-Za-z]+/);return m?m[0].toUpperCase():'';}export function getCategoryFromRef(ref:string){const prefix=getPrefix(ref);const keys=Object.keys(CATEGORY_PREFIX_MAP).sort((a,b)=>b.length-a.length);const k=keys.find(key=>prefix.startsWith(key));return k?CATEGORY_PREFIX_MAP[k]:DEFAULT_CATEGORY;}export const CATEGORY_DISPLAY=CATEGORY_ORDER as const;export type CategoryDisplay=typeof CATEGORY_DISPLAY[number];export function isCategory(value:string):value is CategoryDisplay{return (CATEGORY_DISPLAY as readonly string[]).includes(value);}